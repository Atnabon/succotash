{
  "name": "yet-another-portfolio",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "dotenv": "^17.2.2",
      "zod": "^4.1.13",
      "typescript": "^5"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types",
    "dev:native": "bun run --filter native dev",
    "dev:web": "bun run --filter web dev"
  },
  "packageManager": "bun@1.3.3",
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@yet-another-portfolio/env": "workspace:*"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@yet-another-portfolio/config": "workspace:*",
    "typescript": "catalog:"
  }
}
